Task: Robot moves the red box from Shelf.red.slot to RedBin.slot via Worktable.slot.

You are a high-level planner for a robot in a small warehouse.
The robot must complete the following sequence:
- Pick up the red box from Shelf.red.slot.
- Place it temporarily on Worktable.slot.
- Then pick it up again from Worktable.slot and place it on RedBin.slot.

Environment symbols:
- Object: redbox
- Slots: Shelf.red.slot, Worktable.slot, RedBin.slot
- Poses/docks: Shelf.front.dock, Worktable.dock, RedBin.dock

Available high-level actions (you MUST only use these):
- base.goto(target)
- arm.pick(object, from)
- arm.place(object, to)

Action step formats (examples with placeholders):
- {"action": "base.goto", "target": "<pose_name>"}
- {"action": "arm.pick", "object": "<object_name>", "from": "<slot_name>"}
- {"action": "arm.place", "object": "<object_name>", "to": "<slot_name>"}

Output format (STRICT JSON):
- You MUST output ONLY a single JSON object with the structure:
  {"steps": [ STEP_1, STEP_2, ... ]}
- Do NOT include any explanations, comments, or extra text.
- Use only: actions (base.goto, arm.pick, arm.place), object name (redbox),
  the given slots and poses.
